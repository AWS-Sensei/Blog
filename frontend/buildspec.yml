version: 0.2

env:
  variables:
    HUGO_VERSION: "0.124.1"

phases:
  install:
    commands:
      - echo "Installing Hugo $HUGO_VERSION..."
      - curl -L https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_Linux-64bit.tar.gz -o hugo.tar.gz
      - tar -xzf hugo.tar.gz
      - mv hugo /usr/local/bin/
  build:
    commands:
      - echo "Build Hugo Website..."
      - cd frontend
      - hugo --minify
  post_build:
    commands:
      - echo "Deploying to $WEBSITE_BUCKET..."
      - aws s3 sync public/ s3://$WEBSITE_BUCKET --delete --exclude "audio/*" --exclude "_content/*"
      - echo "Syncing changed markdown files for Polly triggers..."
      - |
        git diff-tree --no-commit-id -r --name-only HEAD -- content/posts/ | grep "\.md$" | while read file; do
          echo "Uploading $file..."
          aws s3 cp "$file" "s3://$WEBSITE_BUCKET/_$file"
        done
      - echo "Invalidating CloudFront cache..."
      - aws cloudfront create-invalidation --distribution-id $CLOUDFRONT_DISTRIBUTION_ID --paths "/*"
